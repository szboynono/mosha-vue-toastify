language: node_js
node_js:
  - 15
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
  - yarn
deploy:
  provider: script
  script:
    - yarn docs:build
    - bash deploy.sh
  on:
    branch: test